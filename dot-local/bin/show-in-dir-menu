#! /bin/sh

chosenFile=$(
    fd | fzf-popup \
        --no-multi --margin 0,1,1 \
        --prompt '~/' --input-label ' Show a file in lf ' \
        --input-border horizontal \
)

# Open the file's location with lf
if [[ -n "$chosenFile" ]]; then
    kitty --detach lf "$HOME/$chosenFile"
fi
